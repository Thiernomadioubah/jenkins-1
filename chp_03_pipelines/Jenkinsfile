pipeline {
    agent any
    triggers {
        // pollSCM('H */4 * * 1-5')
        cron('H */4 * * 1-5')
        // upstream(upstreamProjects: 'job1,job2', threshold: hudson.model.Result.SUCCESS)
    }
    stages {
        stage('exemple') {
            steps {
                echo 'Bonjour le monde'
            }
        }
    }
}